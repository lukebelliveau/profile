import React from 'react';

import { container, text } from './Games.css';

const Games = () => (
  <div className={ container }>
    <p>These are not games, but they will be</p>
  </div>
);

export default Games;
